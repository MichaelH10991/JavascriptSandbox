let array1 = ["a", "b", "c"]
let array2 = ["d", "e", "f"]

// concat method creates a new array with
console.log(array1.concat(array2))

// get the index of element
let pos = array1.indexOf("b")

// remove element from array
array1.splice(1, 1)
array1.splice(1, 1)
console.log(array1)
